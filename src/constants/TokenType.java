package constants;

public enum TokenType {
    IDENT, NUMBER, STRING,
    STAR, COMMA, LPAREN, RPAREN, DOT,
    PLUS, MINUS, MUL, DIV, MOD,
    EQ, NEQ, LT, GT, LE, GE,
    AND, OR, NOT, LIKE, IN, AS,
    EOF
}
